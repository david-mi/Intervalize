import { createStyleSheet } from "react-native-unistyles";

export const styles = createStyleSheet({
  container: {
    // marginTop: 30,
  },
  intensityOverlay: {
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
})