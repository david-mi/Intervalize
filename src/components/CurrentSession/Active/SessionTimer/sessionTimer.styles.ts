import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderWidth: 5,
    borderColor: "black",
    flexDirection: "row"
  },
  title: {
    fontSize: 30,
    backgroundColor: "white",
    textAlign: "center"
  }
})